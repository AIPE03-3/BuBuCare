# 跌倒判斷本地評估報告

日期：2026-07-28
分支：`test/local-pose-eval`
範圍：pose + AcT 主鏈的量化評估。**沒有動正式管線**（`inference_test.py` 一行未改）。

---

## 一句話結論

現行判斷邏輯**抓跌倒很準（召回 95%）但誤報極高（56.5%）**；改成 `geo-first`
（AcT 不能單獨觸發）可把誤報降到 12.1%，代價是多漏 1 段。

---

## 為什麼做這件事

正式管線 `inference_test.py` 三顆模型全部打 Triton，其中 `rt_detr` 是 Blackwell 專屬
TensorRT `.plan`，macOS 開發機起不來 —— 等於**在自己電腦上沒辦法驗證判斷準不準**。

而 AcT 其實本地跑得起來：`ai/action_transformer.pth` 是純 PyTorch state_dict，架構就寫在
`inference_test.py:299-311`，本地建同一個架構載進去即可。pose 用 `yolo11s-pose.pt`。
只有 rt_detr 是真的跑不動。

---

## 產出的工具

| 檔案 | 用途 |
|---|---|
| `ai/local_pose_eval.py` | 只到 pose，看「輸入品質」：偵測率、特徵有效率、關鍵點缺失排行 |
| `ai/local_pipeline_eval.py` | pose + AcT 完整鏈，即時畫面 + 量化指標 |
| `ai/requirements-local-eval.txt` | 依賴（獨立 `ai/.venv`，不污染 backend 的 `.venv`） |

### 判斷邏輯逐項對齊正式管線

改任何一項都會讓結論套不回正式管線，故全部照抄：

| 項目 | 正式管線位置 |
|---|---|
| `conf=0.45` | `inference_test.py:564` |
| 跳幀 `frame_count % 2` | `:536` |
| 選人 `conf × 框面積`（**不是**取信心最高） | `:672-678` |
| 34 維特徵 `kp[:17,:2].flatten()` | `:682` |
| 躺平 `角度<40°` 或 `w/h>1.25` | `:697` |
| 遮擋 `h/normal_h<0.70` 且 `y2>img_h/2` | `:702` |
| 視窗 30 幀、softmax/argmax | `:718-733` |
| 觸發條件三選一 | `:744-749` |

> 重構觸發邏輯時，用**窮舉 224 種輸入組合**驗證 `--trigger-mode current` 與改動前
> 完全等價（0 種不一致），確保基準線可信。

---

## 測試素材

| | |
|---|---|
| 影片 | `ai/test_demo/test4.mp4`（123 MB，**不進版控**，見 `.gitignore` 白名單制） |
| 規格 | 720×480、20 fps、6.2 分鐘、7408 幀 |
| 內容 | 室內單人反覆跌倒演練，含**夜視黑白**與**彩色**兩種成像 |
| 標註 | `ai/test_demo/test4tp.txt`，人工標 **20 段**真跌倒、共 126 秒（佔全片 34%） |

**標註已包含「跌倒後躺在地上」的時間**（經作者確認），所以誤報數字不含「人還躺著系統照報」
那種語意爭議，全是人已正常活動時的誤報。

---

## 指標定義（決定數字誠不誠實）

- **段級召回率** —— 每段真跌倒有沒有**至少一幀**報到。漏一次可能出人命，段才是有意義的單位。
- **幀級誤報率** —— 分母**只算 evaluable 幀**（視窗已滿 + 曾看到人）。把 Buffering 那些
  系統本來就不該報的幀算進分母，會把誤報率洗淡。
- **反應延遲** —— 該段開始到第一次觸發隔幾秒。急救場景延遲就是傷害。

---

## 三種觸發策略實測

同一支影片、同一份標註：

| 模式 | 段級召回 | 幀級誤報 | 平均延遲 | 漏掉的段 |
|---|---|---|---|---|
| `current`（正式管線現況） | **95.0%** (19/20) | 56.5% | 0.26s | #20 |
| **`geo-first`** ⭐ | 90.0% (18/20) | **12.1%** | 0.64s | #19, #20 |
| `geo-strict` | 65.0% (13/20) | 7.1% | 0.91s | #7,10,14,16,18,19,20 |

三種模式的差別只有兩個布林旗標，故實作攤成資料表而非三份 if/else：

```python
"current":    {"act_alone": True,  "occluded_needs_act": False},
"geo-first":  {"act_alone": False, "occluded_needs_act": False},
"geo-strict": {"act_alone": False, "occluded_needs_act": True},
```

### 誤報成因分析

`current` 模式下 1354 個誤報幀的來源：

| 成因 | 佔比 |
|---|---|
| **幾何完全正常、純 AcT 判的** | **78.7%** |
| 幾何判 OCCLUDED | 18.6% |
| 幾何判 LYING | 14.7% |

主因明確：**AcT 在人站著走路時判 FALL，而且信心常常滿分**。所以 `geo-first`
（砍掉 AcT 單獨觸發那條）才會這麼有效。

### geo-first 的殘餘誤報（12.1%，289 幀）

| | 佔殘餘誤報 |
|---|---|
| 只有 OCCLUDED 觸發 | 31.1% |
| 只有 LYING 觸發 | 12.8% |
| 兩者都成立 | 56.1% |

OCCLUDED 涉及 87%。它的規則是「人變矮且位置偏下」，但**人蹲下、坐下、彎腰撿東西、
走到家具後面全都會中**，正常時段有 10.5% 的幀被判成 OCCLUDED。

---

## 走過的錯路（避免後人重走）

### ❌ 判斷一：「AcT 輸出跟真實狀態基本沒相關性」

**錯了。** 這是在還沒有 ground truth 時，看「AcT 與幾何的一致性」推論出來的
（AcT 說 FALL 但幾何說正常佔 72.1%）。拿到人工標註後真實召回率是 **95%** ——
AcT 抓得到跌倒，只是太敏感。**沒有標註就不要下準確率的結論。**

### ❌ 判斷二：「提高信心門檻沒用」

**錯了。** 原因同上，當時憑信心分佈猜測（判 FALL 時 97.5% 的 conf > 0.55）。
實際掃描顯示：門檻拉到 0.95，**召回率完全不變（95%）**、誤報從 56.5% 降到 41.3%。

### ❌ 判斷三：「時間尺度不符是主因」

**只對了一小部分。** `FPS_NOTES.md:250` 提到跳幀會讓 30 幀視窗涵蓋的時間飄掉，
實測四支影片確實不一致：

| 影片 | fps | 跳幀後 | 30 幀視窗涵蓋 |
|---|---|---|---|
| test1 | 24.0 | 12.0/s | 2.50 秒 |
| test2 | 33.0 | 16.5/s | 1.82 秒 |
| test3 | 30.7 | 15.4/s | 1.95 秒 |
| test4 | 20.0 | 10.0/s | **3.00 秒** |

但 `--no-skip` 實驗（視窗 1.5 秒）只把觸發率從 63.0% 降到 53.1% ——
**有影響但不是主因**。

### ❌ 判斷四：「geo-strict 應該會更好」

**錯了，而且錯得最貴。** 動機看起來很合理（OCCLUDED 涉及殘餘誤報的 87%），
實測卻是用 5 個百分點的誤報改善換掉 **5 次真跌倒**。

原因：**真跌倒時 OCCLUDED 本來就常成立**（人倒下被家具擋住），要求 AcT 附議
等於連真的一起砍。

> 這個模式**刻意保留在程式碼裡**並附註失敗原因 —— 它看起來太合理了，不留記錄
> 下一個人還會再試一次。

---

## 漏報分析：#19 / #20

兩段都在影片末尾（336s、346s）。抽幀檢視發現：**人趴跪在地上，且被椅子部分遮擋**。

不是平躺，所以軀幹角與寬高比都不符合「躺平」特徵；又被遮擋，關鍵點缺失。
這是幾何判斷的真實盲區 —— 而且正好是 AcT 應該補上的那種情況。

**這給了重訓一個明確目標**：讓 AcT 學會非典型跌倒姿勢（趴、跪、部分遮擋），
而不是籠統的「提升準確率」。

---

## 建議

| 優先 | 動作 | 說明 |
|---|---|---|
| 1 | **補更多測試影片** | 目前結論只有 test4 一支影片支撐。不同房間／光線／人各拍一支，結論才敢說穩 |
| 2 | **把 `geo-first` 帶回正式管線** | 唯一能真正改善線上行為的一步。應為獨立 PR 讓組員 review |
| 3 | **重訓 AcT** | 目標已明確（非典型姿勢）。注意現有主動學習資料集餵不了 AcT，見下 |

### ⚠ 現有 MLOps 流程餵不了 AcT

`agent/sample_store.py` 收的是「單張快照 + sidecar JSON」，那整套是為 **YOLO-Pose**
設計的。AcT 吃的是完全不同的東西：

| | 現有資料集 | AcT 需要 |
|---|---|---|
| 輸入 | 單張圖片 | **連續 30 幀**的 34 維特徵序列 |
| 標註 | 關鍵點座標 | 這段序列**是不是跌倒**（二分類） |

好消息是 AcT 的訓練資料可以**從影片自動生成**（跑 pose 抽特徵 → 滑動視窗切序列 →
用時間區間標註自動貼標籤），**只需要人標時間區間，不必逐幀標關鍵點**。
本次的 `test4tp.txt` 格式即可直接用作訓練標籤。

另注意：`ai/export_models.py`（`.gitignore:47` 有提到）**實際上不存在**，
重訓後要部署到 Triton 得先補上匯出腳本，且需要有 GPU 那台機器操作。

---

## 怎麼重現

```bash
# 一次性：建環境（ai/.venv 已被 .gitignore 涵蓋）
uv venv ai/.venv
uv pip install --python ai/.venv/bin/python -r ai/requirements-local-eval.txt

# 量化評估（約 4 分鐘）
ai/.venv/bin/python ai/local_pipeline_eval.py ai/test_demo/test4.mp4 \
  --labels ai/test_demo/test4tp.txt --no-show --print-every 99999 \
  --trigger-mode geo-first

# 看畫面（右上角有 frame/時間；落在標註區間會顯示 GT: FALL）
ai/.venv/bin/python ai/local_pipeline_eval.py ai/test_demo/test4.mp4 \
  --labels ai/test_demo/test4tp.txt --no-latch --start 5:30

# 導出逐幀 CSV，可離線掃描參數而不必重跑推論
ai/.venv/bin/python ai/local_pipeline_eval.py ai/test_demo/test4.mp4 \
  --labels ai/test_demo/test4tp.txt --no-show --csv ai/pose_eval_out/test4.csv
```

> **CSV 很重要**：掃四種觸發策略用 CSV 離線算只要幾秒，重跑推論要 4 分鐘 × 4。
> 任何「調參數會怎樣」的問題都應該先用 CSV 算，確認值得再實作。

### 標註檔格式

```
0:03-0:09  0:13-0:20  0:35-0:42     ← 全部黏一行也可以，regex 掃描
frame 6380-6450                      ← 也接受直接寫幀號
# 井號開頭是註解
```

---

## 已知限制

| 限制 | 說明 |
|---|---|
| 只有一支影片 | 所有結論都基於 test4.mp4，不同場景可能完全不同 |
| 不含 detr 那幾條防線 | 離床、座椅滑落、環境巡檢需要 rt_detr，本地跑不動 |
| 標註是單人主觀判定 | 「跌倒何時開始／結束」沒有第二人複核 |
| 誤報率對標註邊界敏感 | 標註區間差幾秒，誤報率就會跟著變 |
