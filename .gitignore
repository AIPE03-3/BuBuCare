# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
fulilian.db
.pytest_cache/
*.pyc

# Virtual environments
.venv

# Environment variables (contains secrets)
.env

# 忽略所有官方與時序的模型權重檔（防止大檔案弄爆 GitHub）
*.pt
*.pth

# 執行時產生的 log（ClearML agent、訓練紀錄等機器產物）
*.log
.clearml-logs-final/

# 忽略作業系統暫存雜訊
.DS_Store
Thumbs.db

# 個人工具檔（skill / 專案說明 / 設定）
.claude/
CLAUDE.md
.vscode/

# 本機 Docker 版本覆蓋（只為了繞開個人環境的相容性問題，不影響 docker-compose.yml 主檔）
docker-compose.override.yml

gcp_vm_environment/.env

# 串流：執行檔與含攝影機帳密的實際設定不進 git（範本 mediamtx.yml.example 才進）
streaming/mediamtx.exe
streaming/mediamtx.yml
streaming/*.log
streaming/*.zip
streaming/*.crt
streaming/*.key